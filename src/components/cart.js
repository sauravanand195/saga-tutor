import { Link, Route, Routes } from 'react-router-dom'

function Cart() {
    return (
        <div>
            <Link to='/'>Product List</Link>
            <h1>Cart Page</h1>
        </div>
    );
}

export default Cart;
